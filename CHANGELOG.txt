# Changelog

## macchina v5.0.0, codename "sorry for breaking your config, again."

- `--no-ascii` and `--custom-ascii` have been ported to the theme mechanism.
- themes: the `color` option has been renamed to `key_color` to avoid ambiguity.
